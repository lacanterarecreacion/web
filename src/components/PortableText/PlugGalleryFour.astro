---
const props = Astro.props;
import { urlForImage } from "@/lib/urlForImage";
---

<div class="grid grid-cols-2 gap-0.5">
  {
    props.images.imagesGallery.map((i: any) => {
      return (
        <a
          class="glightbox group duration-300 hover:opacity-80 relative"
          href={urlForImage(i.asset).width(1300).url()}
        >
          <span class="absolute group-hover:opacity-100 group-hover:scale-110 duration-300 opacity-70 right-0 bottom-0 m-3 rounded-md p-1 bg-gray-900/70 shadow-sm">
            <svg
              xmlns="http://www.w3.org/2000/svg"
              width="25"
              height="25"
              viewBox="0 0 24 24"
            >
              <path
                fill="#fff"
                d="M5 19v-6h2v4h4v2H5Zm12-8V7h-4V5h6v6h-2Z"
              />
            </svg>
          </span>
          <img
            class="object-cover w-full overflow-hidden h-[400px]"
            src={urlForImage(i.asset).width(600).height(600).url()}
          />
        </a>
      );
    })
  }
</div>

<!-- Initialize Swiper -->
<!-- <script>
  //@ts-ignore
  import Swiper from "swiper/bundle";
  import "swiper/css/bundle";
  //@ts-ignore
  const swiper = new Swiper(".mySwiper", {
    pagination: {
      el: ".swiper-pagination",
      clickable: true,
    },
    slidesPerView:1,
    autoplay: {
      delay: 2500,
      disableOnInteraction: false,
    },
    navigation: {
      nextEl: ".swiper-button-right",
      prevEl: ".swiper-button-left",
    },
  });
</script>

<style>
  .swiper {
    @apply w-full select-none;
  }

  .swiper-button-left,
  .swiper-button-right {
    @apply bg-white w-12 h-12 absolute top-1/2 z-50 flex justify-center items-center -translate-y-10;
  }

  .swiper-button-right {
    @apply right-0;
  }
</style> -->
