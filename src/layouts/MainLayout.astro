

<main
  class="flex flex-col w-full min-h-screen overflow-x-hidden relative max-w-screen pt-24 overscroll-auto scroll-smooth"
>
  <slot />
</main>

<script>
  // @ts-ignore
  import GLightbox from "glightbox";
  document.addEventListener("DOMContentLoaded", () => {
    const glightbox = GLightbox({
      /* options */
    });
  });
</script>