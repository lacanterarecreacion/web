---
import IconRollingDice from "@/components/Icons/IconRollingDice.astro"
---

<main
  class="relative flex flex-col w-full pt-12 overflow-x-hidden max-w-screen xl:pt-20 scroll-smooth"
>
  <slot />

  <a href="/contacto" class="fixed group right-0 bottom-0 bg-gray-100  w-16 m-6 flex justify-center items-center rounded-full cursor-pointer z-50 shadow h-16">
    <IconRollingDice/>
  </a>
</main>

<script>
  // @ts-ignore
  import GLightbox from "glightbox";
  document.addEventListener("DOMContentLoaded", () => {
    const glightbox = GLightbox({
      /* options */
    });
  });
</script>