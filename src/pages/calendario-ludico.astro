---
import FullCalendar from "@/components/Calendar/FullCalendar.vue";
import Layout from "@/layouts/CalendarLayout.astro";
import { useSanityClient } from "astro-sanity";
import { Image } from "@astrojs/image/components";
import LogoCantera from "@/components/Icons/LogoCantera.astro";

const calendario = await useSanityClient().fetch(
  `
  *[_type == "calendarioLudico"] | order(start asc)
    {
      title,
      start,
      end,
      "extendedProps":  {
        description,
        image,
        link, 
      }
    }`
);

---

<Layout title="Calendario lúdico">
  <div class="sm:p-1 mt-0 min-h-screen bg-gradient-to-br from-indigo-100 bg-amber-100" >
    <FullCalendar calendarEvents={calendario} client:load >
      <a href="/" rel="prefetch" class="md:relative absolute top-8 md:top-0 left-2 flex justify-start flex-col items-center pl-1 md:pl-0 ">
        <div class="w-40 md:w-32">
          <LogoCantera/>
        </div>
      </a>
    </FullCalendar>
  </div>
</Layout>
