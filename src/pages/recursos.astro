---
import Layout from "@/layouts/Layout.astro";
import { useSanityClient } from "astro-sanity";
import GrainOne from "@/components/Grain/GrainOne.astro";
import StrackedScene from "@/components/UI/Backgrounds/StrackedScene.astro";
const calendario = await useSanityClient().fetch(
  `*[_type == "calendarioLudico"]`
);
---

<Layout title="Recursos">
  <div
    class="relative flex items-center justify-center pt-32 pb-32 overflow-hidden bg-gradient-to-tr from-teal-400 bg-emerald-600"
  >
    <h1 class="relative z-10 font-mono text-4xl text-white md:text-6xl">Recursos</h1>
    <div class="absolute inset-0 z-0 opacity-90">
      <GrainOne />
    </div>
    <div class="absolute inset-0 z-0 opacity-30">
      <StrackedScene/>
    </div>
  </div>
  {
    calendario && (
      <div class="grid max-w-2xl min-h-screen gap-6 mx-auto mt-32 lg:grid-cols-1">
        {calendario.map((post: any) => (
          <a href="#">
            <span class="text-2xl">{post.title}</span>
          </a>
        ))}
      </div>
    )
  }
</Layout>
